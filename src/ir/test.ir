@a = global u32 1
@b = global u32 2

back:
    %1 = alloca u32
    %2 = load * @a
    store %2, * %1
    %3 = load * @b
    store %3, * @a
    %4 = load * %1
    store %4, * @b

    %5 = load * @a
    %6 = load * @b
    blt %5, %6, back, next
next:
    %7 = add %5, %6
    j back
